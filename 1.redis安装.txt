docker pull redis:latest

下载redis.conf 放在 /usr/local/redis/redis.conf: 下

docker run -d -p 6379:6379 -v /usr/local/redis/redis.conf:/etc/redis/redis.conf -v /usr/local/redis/data:/data --name testredis redis redis-server /etc/redis/redis.conf

docker exec -it redis-test /bin/bash

启动
redis-cli

关于修改redis.conf文件后 docker 中配置如何生效问题

docker restart testredis   重启redis容器即可